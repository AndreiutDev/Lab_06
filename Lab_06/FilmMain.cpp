#include<iostream>
#include <stdio.h>
#include "ValidatorClass1.h"
#include "FilmUI.h"

#include <fstream>

using namespace std;

int main(int argc, char** argv)
{
    FilmUI menu;

    menu.Login();


    /*int key;
    bool penice = false;
    ExceptionClass ex;
    ValidatorClass cioaba;
    while(penice == false)
   {
        if (cin >> key)
        {
            penice = true;
        }
        else
        {
            penice = false;
        }
        try
        {
            cioaba.validateInteger(penice);
        }
        catch (...)
        {
            cout << "lobi";
        }
        cin.clear();
        std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');*/
    
}